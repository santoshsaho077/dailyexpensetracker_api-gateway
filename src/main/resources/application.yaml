spring:
  application:
      name: api-gateway
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:8080
          predicates:
            - Path=/signUp/**, /signIn/**
  config:
    import: optional:configserver:http://localhost:9090/

server:
  port: 9000